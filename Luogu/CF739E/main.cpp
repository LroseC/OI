#include <iostream>

using i64 = long long;

int main(void)
{
	std::cin >> n >> cnta >> cntb;
	for (int i = 1; i <= n; ++i)
		std::cin >> a[i];
	for (int i = 1; i <= n; ++i)
		std::cin >> b[i];
	double l = -1e4, r = 1e4;
	while (r - l > eps)
	return 0;
}
