#include <cstdio>
#include <iostream>

using namespace std;

int CASEID;
int n, m, q;

int main(void)
{
	ios::sync_with_stdio(0);
	cin >> CASEID;
	cin >> n >> m >> q;
	for (int i = 1; i <= n; ++i) cin >> w[i];
	return 0;
}
