#include <cstdio>

using namespace std;

const int M = 1e4 + 10, N = M << 1;

struct PII
{
	int 

int n, m;
int l, r;
char str[10];

int main()
{
	scanf("%d%d" ,&n, &m);
	for (int i = 1; i <= m; ++i) {
